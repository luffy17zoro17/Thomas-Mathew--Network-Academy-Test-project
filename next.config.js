/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains:[`networkacademy.org`,
      `images.unsplash.com`],
  }

}

module.exports = nextConfig
